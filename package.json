{
  "name": "truth-table-builder",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight single-file truth table builder with auto-evaluation.",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'"
  }
}
