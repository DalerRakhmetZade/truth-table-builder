{
  "name": "truth-table-builder",
  "version": "1.7.0",
  "private": true,
  "type": "module",
  "description": "A lightweight truth table builder with practice mode and auto-evaluation.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test 'tests/**/*.test.mjs'",
    "sync-version": "node scripts/sync-version.mjs"
  }
}
